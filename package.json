{
  "name": "midday",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*",
    "tooling/*"
  ],
  "scripts": {
    "build": "turbo build",
    "clean": "git clean -xdf node_modules",
    "clean:workspaces": "turbo clean",
    "dev": "turbo dev --parallel",
    "dev:app": "turbo dev --filter=@midday/app",
    "format": "biome format --write .",
    "lint": "turbo lint && manypkg check",
    "check:types": "turbo typecheck"
  },
  "dependencies": {
    "@biomejs/biome": "1.2.2",
    "@manypkg/cli": "^0.21.0",
    "turbo": "^1.10.15",
    "typescript": "^5.2.2"
  }
}
