# --- Domains used by Traefik + Dokploy (Cloudflare or other certresolver) ---
# The compose uses these in Traefik labels
API_DOMAIN=api.example.com
APP_DOMAIN=app.example.com
TRAEFIK_NETWORK=traefik_proxy
TRAEFIK_CERTRESOLVER=cloudflare

# --- Public URLs used by the app ---
# Set to the HTTPS domains above (used by Next.js client)
NEXT_PUBLIC_URL=https://app.example.com
NEXT_PUBLIC_API_URL=https://api.example.com

# --- Supabase ---
# Public keys for client (Next.js) and server keys for API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
SUPABASE_JWT_SECRET=

# --- Database (managed Postgres recommended) ---
# Primary DB connection used for writes
DATABASE_PRIMARY_URL=
# Optional read replicas (point to primary if not used)
DATABASE_FRA_URL=
DATABASE_IAD_URL=
DATABASE_SJC_URL=
# Drizzle CLI config expects this; can point at a pooler
DATABASE_SESSION_POOLER=

# --- Email (Resend) ---
RESEND_API_KEY=

# --- CORS / API ---
# Comma-separated allowed origins; set to your app domain(s)
ALLOWED_API_ORIGINS=https://app.example.com
PORT=3000

# --- Misc ---
NODE_ENV=production
TZ=UTC

