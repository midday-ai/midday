name: Preview Deployment - API
permissions:
  contents: read
on:
  push:
    branches-ignore:
      - main
    paths:
      - apps/api/**
      - packages/**
      - "!packages/email/**"
      - "!packages/ui/**"
jobs:
  deploy:
    name: 🚀 Deploy API to Railway Staging
    runs-on: ubuntu-latest
    concurrency: railway-api-staging
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: v1.3.9
      - name: Install dependencies
        run: bun install
      - name: 🏗 Build @midday/engine
        run: bunx turbo run build --filter=@midday/engine...
      - name: 🧪 Run tests
        run: bunx turbo run test --filter=@midday/api...
      - name: 🔦 Run linter
        run: bun run lint
        working-directory: ./apps/api
      - name: 🪐 Check TypeScript
        run: bun run typecheck
        working-directory: ./apps/api
      - name: Install Railway CLI
        run: npm install -g @railway/cli
      - name: 🚀 Deploy to Railway Staging
        run: railway up --service ${{ secrets.RAILWAY_API_STAGING_SERVICE_ID }} --environment staging
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
