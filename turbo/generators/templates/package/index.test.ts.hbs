import { hello } from '../src/index'; describe('@packages/{{kebabCase name}}',
() => { it('should return the correct greeting', () => {
expect(hello()).toBe('Hello from @packages/{{kebabCase name}}!'); }); });