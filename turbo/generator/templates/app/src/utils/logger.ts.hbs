/**
 * Logs a message to the console with optional parameters.
 *
 * @param message - The main message to be logged.
 * @param params - Optional parameters to be logged along with the message.
 */
export const logger = (message: string, params?: unknown) => {
  console.log(message, params);
};