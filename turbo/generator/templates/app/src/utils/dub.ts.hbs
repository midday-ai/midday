/**
 * @fileoverview This file initializes and exports a Dub instance for use
 * throughout the application.
 * @module utils/dub
 */

import { Dub } from "dub";

/**
 * A singleton instance of the Dub class.
 *
 * @description
 * This constant provides a centralized Dub instance for the entire application.
 * Dub is likely a utility or service that handles specific functionality
 * (e.g., audio dubbing, data transformation, etc.) - the exact purpose
 * would depend on the 'dub' package's implementation.
 *
 * @example
 * // In another file:
 * import { dub } from '../utils/dub';
 *
 * // Use the dub instance
 * dub.someMethod();
 *
 * @constant
 * @type {Dub}
 */
export const dub = new Dub();
