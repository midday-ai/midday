import { Input } from "@midday/ui/input";

export function Search() {
  return (
    <Input
      placeholder="Search for or jump to"
      className="bg-transparent max-w-[500px] border-0"
    />
  );
}
