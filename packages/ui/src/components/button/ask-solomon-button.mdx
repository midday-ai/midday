import { Canvas, Controls, Meta, Source } from "@storybook/blocks";

import * as AskSolomonAiButton from "./ask-solomon-button.stories";

<Meta of={AskSolomonAiButton} />

# AskSolomonAiButton Component

The `AskSolomonAiButton` is a button component styled to stand out and attract users to interact with it. It can be used for important call-to-action buttons, such as asking queries or initiating AI-based solutions.

## Component Props

<Canvas of={AskSolomonAiButton.Default} />

<Controls />

## Usage Example

Here's a basic usage example of the `AskSolomonAiButton`:

```tsx
<Canvas>
  <Story name="Default">
    <AskSolomonAiButton
      href="https://example.com"
      label="Click me"
      active={true}
    />
  </Story>
</Canvas>
```
