import { Canvas, Controls, Meta, Source } from "@storybook/blocks";

import * as PaymentPortalButton from "./payment-portal-button.stories";

<Meta of={PaymentPortalButton} />

# PaymentPortalButton Component

The `PaymentPortalButton` is a button component styled to stand out and attract users to interact with it. It can be used for important call-to-action buttons, such as asking queries or initiating AI-based solutions.

## Component Props

<Canvas of={PaymentPortalButton.Default} />

<Controls />

## Usage Example

Here's a basic usage example of the `PaymentPortalButton`:

```tsx
<Canvas>
  <Story name="Default">
    <PaymentPortalButton
      href="https://example.com"
      label="Click me"
      active={true}
    />
  </Story>
</Canvas>
```
