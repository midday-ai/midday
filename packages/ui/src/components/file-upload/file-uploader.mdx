import { Canvas, Controls, Meta, Source } from "@storybook/blocks";

import * as FileUploader from "./file-uploader.stories";

<Meta of={FileUploader} />

# FileUploader Component

The `FileUploader` is a component for uploading files.

## Component Props

<Canvas of={FileUploader.Default} />

<Controls />

## Usage Example

Here's a basic usage example of the `FileUploader`:

```tsx
<Canvas>
  <Story name="Default">
    <FileUploader href="https://example.com" label="Click me" active={true} />
  </Story>
</Canvas>
```
