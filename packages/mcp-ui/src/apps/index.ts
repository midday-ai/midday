// Export registry (server-side compatible - no DOM dependencies)
export * from "./registry";

// Note: Other exports require DOM types and should only be imported in browser environments
// - resource-factory: import from "@midday/mcp-ui/apps/resource-factory"
// - mcp-handler: import from "@midday/mcp-ui/apps/mcp-handler"
