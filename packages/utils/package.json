{
    "name": "@midday/utils",
    "version": "1.0.0",
    "main": "src/index.ts",
    "private": true,
    "sideEffects": false,
    "scripts": {
        "clean": "rm -rf .turbo node_modules",
        "lint": "biome check .",
        "format": "biome --write .",
        "check:types": "tsc --noEmit"
    },
    "devDependencies": {
        "typescript": "^5.4.3"
    }
}
