import { baseConfig } from "./config-base";

// Client-side config - imported by Next.js dashboard
// Note: No preview images yet - add e-invoicing.jpg when available
export default {
  ...baseConfig,
  images: [] as string[],
};

