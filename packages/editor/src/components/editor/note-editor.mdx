import { Canvas, Controls, Meta, Source } from "@storybook/blocks";

import * as NoteEditor from "./note-editor.stories";

<Meta of={NoteEditor} />

# NoteEditor Component

The `NoteEditor` is a note editor component that can be used to edit a note.

## Component Props

<Canvas of={NoteEditor.Default} />

<Controls />

## Usage Example

Here's a basic usage example of the `NoteEditor`:

```tsx
<Canvas>
  <Story name="Default">
    <NoteEditor href="https://example.com" label="Click me" active={true} />
  </Story>
</Canvas>
```
